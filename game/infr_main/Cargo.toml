[package]
name = "infr_main"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.0", default-features = false, features = [
    "2d",
    "wayland",
    "dynamic_linking",
    "debug",
] }
infr_client = { path = "../infr_client" }
infr_render = { path = "../infr_render" }
infr_res = { path = "../infr_res" }
