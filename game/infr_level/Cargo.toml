[package]
name = "infr_level"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.0", default-features = false, features = ["bevy_text"] }
infr_client = { path = "../infr_client/" }
infr_transfer = { path = "../../server/infr_transfer/" }
ron = "0.12.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
