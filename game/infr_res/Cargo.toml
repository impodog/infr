[package]
name = "infr_res"
version = "0.1.0"
edition = "2024"

[dependencies]
infr_client = { path = "../infr_client" }
bevy = { version = "0.18.0", default-features = false, features = [
    "png",
    "bevy_sprite",
] }
rand = "0.9.2"
