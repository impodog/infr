[package]
name = "infr_res"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.0", default-features = false, features = [
    "png",
    "bevy_sprite",
    "bevy_sprite_render",
    "bevy_asset",
] }
infr_client = { path = "../infr_client" }
rand = "0.9.2"
