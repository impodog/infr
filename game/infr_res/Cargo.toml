[package]
name = "infr_res"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = [
  "bevy_asset",
  "bevy_sprite",
  "bevy_sprite_render",
  "bevy_text",
  "png",
] }
infr_client = { path = "../infr_client" }
rand = "0.10.0"
